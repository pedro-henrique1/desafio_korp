global:
  scrape_interval:     {{ prometheus_scrape_interval }}
  evaluation_interval: {{ prometheus_scrape_interval }}

scrape_configs:
  - job_name: '{{ app_container_name }}'
    static_configs:
      - targets: ['{{ app_container_name }}:{{ app_port }}']
